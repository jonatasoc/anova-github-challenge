import React from 'react';
import { LoadingIcon } from './Loading.styles';

const Loading: React.FC = () => {
  return <LoadingIcon className="lds-dual-ring" size={20} />;
};

export default Loading;
